# Changelog

## [Unreleased]

## [1.0.1] - 2026-08-17

### Changed

- 未全局安装 dsh 时，自动使用官方启动命令 `npx @deepseek-ai/dsh web` 启动（首次运行会自动下载），
  无需手动执行 `npm i -g`
- 默认端口改为 `3080`；默认启动方式改为「在 Windows 中直接启动」
- 未检测到 Node.js 环境（node / npm / npx 均不可用）时不再尝试启动，并提示先安装 Node.js

### Fixed

- 多窗口状态保持一致：同时打开多个 IDE 窗口时，任一窗口启动或停止 dsh，所有窗口的状态显示都会同步刷新，
  不再出现某个窗口显示「未启动」但实际已在运行的情况
- 兼容性改进：修复新版 IDE（2024.2 及以上）打开新窗口时的报错，以及编辑器右键菜单的偶发报错
- 兼容更多 IDEA 版本：插件现在可安装到 IntelliJ IDEA 2022.3 及之后的所有版本（不再有版本上限）

## [1.0.0] - 2026-08-16

### Added

- 右侧工具栏 dsh 鲸鱼图标，点击打开 Dsh Dock 工具窗口
- 一键启动 dsh web（WSL / Windows 两种方式），隐藏后台运行，WebUI 直接显示在工具窗口内嵌的浏览器中
- 项目空间默认使用当前打开的 IDEA 项目根路径
- 启动 / 停止 / 刷新 / 在系统浏览器中打开 / 设置
- 操作日志面板、端口自动探测、打开工具窗口自动启动
- 支持 IDEA 2022.1 ~ 2025.2
- 右键发送代码/文件到 Dsh Dock：编辑器选中代码 → `@路径#L行号`，项目视图选中文件 → `@路径`，
  路径按启动模式自动转换（WSL: `/mnt/...`；Windows: `C:/...`），发送后自动打开工具窗口并填入输入框
- 工作空间跟随当前项目：打开 WebUI 时自动把当前项目设为工作空间，不会停留在上次用过的项目，
  并自动清理遗留的空白会话以保证「新会话」始终可用
- 启动失败提示：未安装 dsh（或启动失败）时快速弹窗提示，并给出安装命令
  `npm i -g @deepseek-ai/dsh`（WSL / Windows 模式均覆盖）
- WebUI 语言跟随 IDE：中文界面下自动切换为中文（不再受浏览器默认语言影响）