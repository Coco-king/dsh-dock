# Changelog

## [Unreleased]

### Added

- 右侧工具栏 dsh 鲸鱼图标，点击打开 Dsh 工具窗口
- 在 WSL 中启动 dsh web（参考 `ldsh.cmd`），隐藏后台运行
- 工具窗口内嵌 JCEF 浏览器加载 WebUI
- 项目空间默认使用当前打开的 IDEA 项目根路径
- 启动 / 停止 / 刷新 / 系统浏览器打开 / 设置
- 日志面板、端口探测、自动启动
- 支持 IDEA 2022.1 (221) ~ 2025.2 (252.*)
- 右键发送代码/文件到 Dsh：编辑器选中代码 → `@路径#L行号`，项目视图选中文件 → `@路径`，
  路径按启动模式自动转换（WSL: `/mnt/...`；Windows: `C:/...`），自动打开工具窗口并注入输入框
- 工作空间跟随当前项目：加载 WebUI 前通过 dsh 的 `/api` RPC（workspace.create / session.create）
  把当前项目同步为会话工作空间，不再停留在上次用过的项目
- 空白会话卫生 + 持久化会话选择清理：归档项目工作空间遗留的空白会话（让"新会话"始终新建并切换），
  并在加载前清除不属于当前项目的持久化会话选择（避免页面恢复旧项目会话）
- 启动失败提示：未安装 dsh（或启动失败）时快速失败并弹出「Dsh 启动失败」通知，
  提示安装命令 `npm i -g @deepseek-ai/dsh`（WSL / Windows 模式均覆盖）